[default]
out = ["Html", "Lcov"]
timeout = "300s"
# Set to the current coverage floor; ratchet upward as test coverage improves.
fail-under = 59.0
exclude-files = ["src/main.rs", "src/authoring/mod.rs", "src/schema/mod.rs"]
